apiVersion: v1
kind: Secret
metadata:
  name: authentik-secret
  namespace: authentik
type: Opaque
stringData:
  # Generate with: openssl rand -hex 32
  secret_key: "REPLACE_WITH_SECRET_KEY"
  # Password for the built-in PostgreSQL instance
  postgresql_password: "REPLACE_WITH_PG_PASSWORD"
