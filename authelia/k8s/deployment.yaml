apiVersion: apps/v1
kind: Deployment
metadata:
  name: authelia
  namespace: authelia
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: authelia
  template:
    metadata:
      labels:
        app: authelia
    spec:
      enableServiceLinks: false
      containers:
        - name: authelia
          image: ghcr.io/authelia/authelia:4.38
          args:
            # Authelia merges these in order. Non-sensitive structure lives in
            # the ConfigMap; anything with a credential in it lives in a Secret,
            # because this repository is public.
            - --config
            - /etc/authelia/configuration.yml
            - --config
            - /secrets/secrets.yml
            - --config
            - /secrets-oidc/oidc.yml
          ports:
            - containerPort: 9091
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          readinessProbe:
            httpGet:
              path: /api/health
              port: 9091
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: 9091
            initialDelaySeconds: 15
            periodSeconds: 30
          volumeMounts:
            - name: config
              mountPath: /etc/authelia/configuration.yml
              subPath: configuration.yml
              readOnly: true
            - name: secrets
              mountPath: /secrets
              readOnly: true
            - name: oidc
              mountPath: /secrets-oidc
              readOnly: true
            - name: data
              mountPath: /config
      volumes:
        - name: config
          configMap:
            name: authelia-config
        - name: secrets
          secret:
            secretName: authelia-secret
        # Holds identity_providers.oidc.clients, including the client secret
        # digest. Created out of band -- see the comment in k8s/configmap.yaml.
        - name: oidc
          secret:
            secretName: authelia-oidc
        - name: data
          persistentVolumeClaim:
            claimName: authelia-config
